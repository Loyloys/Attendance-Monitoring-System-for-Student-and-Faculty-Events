/* eslint-disable react-refresh/only-export-components */
// Backward-compatible import path for components that used the original auth context.
export { AuthProvider, useAuth } from '../context/AuthContext';
